---
phase: 01-foundation-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/app/api/health/route.ts
  - src/lib/db/schema.ts
autonomous: false
requirements:
  - DASH-06

must_haves:
  truths:
    - "Health check endpoint at /api/health returns JSON with status, version, and database connectivity"
    - "Health check returns 200 with database 'connected' when PostgreSQL is reachable"
    - "Health check returns 503 with database 'disconnected' when PostgreSQL is unreachable"
    - "Database schema is pushed to PostgreSQL via drizzle-kit push"
    - "Root page is accessible and displays DevDock branding"
  artifacts:
    - path: "src/app/api/health/route.ts"
      provides: "Health check API endpoint"
      exports: ["GET"]
    - path: "drizzle/"
      provides: "Generated migration or push state"
  key_links:
    - from: "src/app/api/health/route.ts"
      to: "src/lib/db/index.ts"
      via: "import db"
      pattern: "import.*db.*from.*@/lib/db"
    - from: "src/app/api/health/route.ts"
      to: "drizzle-orm"
      via: "sql template tag for SELECT 1"
      pattern: "sql`SELECT 1`"

user_setup:
  - service: postgresql-local
    why: "Local PostgreSQL needed for schema push and health check verification"
    env_vars: []
    dashboard_config:
      - task: "Start local PostgreSQL via docker-compose.dev.yml"
        location: "Run: docker compose -f docker-compose.dev.yml up -d"
---

<objective>
Wire the health check API endpoint, push the database schema to PostgreSQL, and verify the complete Phase 1 foundation works end-to-end. This is the integration and verification plan that proves the skeleton is functional.

Purpose: Validate that the application skeleton (Plan 01) and infrastructure configs (Plan 02) produce a working system — database connected, API responding, UI rendered.
Output: Working health check endpoint and verified database schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-UI-SPEC.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes -->

From src/lib/db/index.ts:
```typescript
export const db: DrizzleInstance; // Drizzle client with schema
```

From src/lib/db/schema.ts:
```typescript
export const users: PgTable;         // users table definition
export const environments: PgTable;  // environments table definition
export const userRoleEnum: PgEnum;
export const environmentStatusEnum: PgEnum;
```

From src/lib/config.ts:
```typescript
export const config: {
  DATABASE_URL: string;
  DOCKER_SOCKET: string;
  DEVDOCK_DATA_DIR: string;
  DEVDOCK_MAX_CONCURRENT_ENVS: number;
  NODE_ENV: 'development' | 'production' | 'test';
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create health check API endpoint and push database schema</name>
  <files>src/app/api/health/route.ts</files>
  <read_first>
    src/lib/db/index.ts
    src/lib/db/schema.ts
    src/lib/config.ts
    .planning/phases/01-foundation-infrastructure/01-RESEARCH.md
    .planning/phases/01-foundation-infrastructure/01-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Ensure local PostgreSQL is running.**

Start the development PostgreSQL container:

```bash
docker compose -f docker-compose.dev.yml up -d
```

If Docker is not accessible (user not in docker group or daemon not running), this is a blocking issue. Check with `docker ps` first. If Docker is not available, the schema push and health check verification cannot proceed — flag for user intervention.

**Step 2: Create `src/app/api/health/route.ts` — health check endpoint.**

Per UI-SPEC copywriting contract for health check responses:

```typescript
import { NextResponse } from 'next/server';
import { db } from '@/lib/db';
import { sql } from 'drizzle-orm';

const VERSION = '0.1.0';

export async function GET() {
  try {
    // Verify database connectivity with a simple query
    await db.execute(sql`SELECT 1`);

    return NextResponse.json(
      {
        status: 'ok',
        version: VERSION,
        database: 'connected',
      },
      { status: 200 }
    );
  } catch {
    return NextResponse.json(
      {
        status: 'error',
        version: VERSION,
        database: 'disconnected',
      },
      { status: 503 }
    );
  }
}
```

Key details:
- Response format matches UI-SPEC exactly: `{ status, version, database }`
- Successful: `{ "status": "ok", "version": "0.1.0", "database": "connected" }` with HTTP 200
- Failed: `{ "status": "error", "version": "0.1.0", "database": "disconnected" }` with HTTP 503
- Version hardcoded as `"0.1.0"` per UI-SPEC
- Error catch block does NOT expose error details in the response (security: no stack traces to clients)
- Uses `sql` template tag from drizzle-orm for the connectivity check

**Step 3: Push database schema to PostgreSQL via drizzle-kit.**

[BLOCKING] This step is mandatory. The phase cannot pass verification without the database schema being applied.

```bash
npx drizzle-kit push
```

This creates the `users` and `environments` tables, the `user_role` and `environment_status` enums, and the `env_slug_user_idx` unique index in the `devdock` PostgreSQL database.

If `drizzle-kit push` prompts for confirmation interactively, it may need to be handled. Drizzle Kit push typically runs non-interactively when there are no destructive changes (first push on empty database).

**Step 4: Verify the schema was applied.**

```bash
# Check tables exist
docker compose -f docker-compose.dev.yml exec postgres psql -U devdock_app -d devdock -c "\dt"
```

Expected output should list `users` and `environments` tables.

**Step 5: Verify TypeScript still compiles after adding the health route.**

```bash
npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /home/mohed_abbas/murx/perso/claudecodevps && npx tsc --noEmit && test -f src/app/api/health/route.ts && grep -q "status.*ok" src/app/api/health/route.ts && grep -q "database.*connected" src/app/api/health/route.ts && grep -q "version.*0.1.0" src/app/api/health/route.ts && echo "PASS: Health route exists, compiles, has correct response shape" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/health/route.ts exists and exports `GET` function
    - src/app/api/health/route.ts imports `db` from `@/lib/db`
    - src/app/api/health/route.ts imports `sql` from `drizzle-orm`
    - src/app/api/health/route.ts returns `{ "status": "ok", "version": "0.1.0", "database": "connected" }` with HTTP 200 on success
    - src/app/api/health/route.ts returns `{ "status": "error", "version": "0.1.0", "database": "disconnected" }` with HTTP 503 on failure
    - src/app/api/health/route.ts does NOT expose error.message or stack traces in the response body
    - `npx tsc --noEmit` exits with code 0
    - `npx drizzle-kit push` has been run successfully (users and environments tables exist in database)
  </acceptance_criteria>
  <done>
    Health check endpoint returns correct JSON shape per UI-SPEC, database schema is applied to PostgreSQL with users and environments tables, TypeScript compiles without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 foundation end-to-end</name>
  <files>src/app/page.tsx, src/app/api/health/route.ts, docker/base/Dockerfile, deploy/nginx/devdock.conf</files>
  <action>
This is a human verification checkpoint. All automation is complete from prior tasks. Present the following verification steps to the user.

**What was built:**
Complete Phase 1 foundation: Next.js 15 application with PostgreSQL database, Drizzle ORM schema, shadcn/ui design system, health check API, Docker base image Dockerfile, Compose templates, nginx config, and systemd service file.

**Verification steps for the user:**

Prerequisites: Local PostgreSQL must be running (`docker compose -f docker-compose.dev.yml up -d`)

1. Start the dev server: `npm run dev`

2. Open http://localhost:3000 in browser:
   - Page should display "DevDock" as a large heading
   - Below it: "Remote development platform" in muted text
   - Below that: "Dashboard coming soon. System is running." in muted text
   - Page should be dark-themed (dark background, light text)
   - Content should be centered both vertically and horizontally

3. Open http://localhost:3000/api/health in browser:
   - Should return JSON: `{"status":"ok","version":"0.1.0","database":"connected"}`
   - If database is not connected, you'll see: `{"status":"error","version":"0.1.0","database":"disconnected"}`

4. Verify TypeScript compiles: `npx tsc --noEmit` (should exit cleanly)

5. Verify build works: `npm run build` (should complete, `.next/standalone/` directory should exist)

6. Inspect key files exist:
   - `docker/base/Dockerfile` — base dev container
   - `docker/templates/base-compose.yml` — Compose template
   - `deploy/nginx/devdock.conf` — nginx config
   - `deploy/systemd/devdock.service` — systemd service

**Resume signal:** Type "approved" if everything looks correct, or describe any issues you see.
  </action>
  <verify>
    <automated>cd /home/mohed_abbas/murx/perso/claudecodevps && npm run build 2>&1 | tail -5 && test -d .next/standalone && echo "PASS: Build produces standalone output" || echo "FAIL: Build or standalone missing"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms root page displays "DevDock" heading with dark theme
    - User confirms /api/health returns JSON with status, version, database fields
    - `npm run build` completes successfully
    - `.next/standalone/` directory exists after build
    - docker/base/Dockerfile exists
    - docker/templates/base-compose.yml exists
    - deploy/nginx/devdock.conf exists
    - deploy/systemd/devdock.service exists
  </acceptance_criteria>
  <done>
    User has verified the complete Phase 1 foundation works: root page renders correctly, health check returns proper JSON, build produces standalone output, and all infrastructure config files exist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/health | Unauthenticated endpoint (intentional for monitoring) |
| health route -> database | Internal trusted connection to PostgreSQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | /api/health error response | mitigate | Error catch block returns generic "disconnected" — never exposes error.message, connection strings, or stack traces |
| T-03-02 | Denial of Service | /api/health database query | accept | SELECT 1 is minimal; health endpoint is public by design for monitoring; rate limiting deferred to Phase 2 auth middleware |
| T-03-03 | Information Disclosure | version in health response | accept | Version "0.1.0" is low-value information; standard practice for health endpoints |
</threat_model>

<verification>
1. `src/app/api/health/route.ts` exists with GET export
2. Health check returns correct JSON shape with status/version/database fields
3. Database tables `users` and `environments` exist in PostgreSQL
4. `npx tsc --noEmit` passes
5. `npm run build` produces `.next/standalone/` directory
6. Root page displays DevDock branding with dark theme
7. All Docker and deployment config files exist in correct locations
</verification>

<success_criteria>
Phase 1 foundation is fully verified: Next.js 15 application serves root page and health check API behind a working dev server, PostgreSQL schema is applied with users and environments tables, TypeScript compiles without errors, standalone build produces a deployable artifact, and all Docker/deployment configs exist with correct security boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md`
</output>
