---
phase: 03-environment-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/lib/db/schema.ts
autonomous: false
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04
  - ENV-05
  - ENV-06
  - ENV-07

must_haves:
  truths:
    - "Database schema matches the code schema (errorMessage column exists in PostgreSQL)"
    - "Application builds without errors"
    - "All unit tests pass"
    - "Dashboard page renders without errors"
    - "User can visually confirm the dashboard UI loads"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Schema with errorMessage column pushed to database"
  key_links:
    - from: "src/lib/db/schema.ts"
      to: "PostgreSQL database"
      via: "npx drizzle-kit push"
      pattern: "errorMessage.*text"
---

<objective>
Push the updated database schema (errorMessage column) to PostgreSQL, run the full build, execute all tests, and perform a human verification checkpoint to confirm the dashboard UI renders correctly.

Purpose: This is the integration gate. All backend (Plan 01+02) and frontend (Plan 03) work converges here. The schema push is a blocking prerequisite -- without it, environment creation will fail at the database level. The build verifies all TypeScript compiles. The checkpoint confirms the UI is visually correct.

Output: Database schema synced, build passing, tests green, human-verified UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-environment-lifecycle/03-CONTEXT.md
@.planning/phases/03-environment-lifecycle/03-01-SUMMARY.md
@.planning/phases/03-environment-lifecycle/03-02-SUMMARY.md
@.planning/phases/03-environment-lifecycle/03-03-SUMMARY.md

@src/lib/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema push, build verification, and test suite</name>
  <files>
    src/lib/db/schema.ts
  </files>
  <read_first>
    src/lib/db/schema.ts,
    vitest.config.ts,
    src/lib/config.ts
  </read_first>
  <action>
    **Step 1: Push database schema**

    Run `npx drizzle-kit push` to sync the schema.ts changes (errorMessage column) to the PostgreSQL database. This is the Drizzle ORM equivalent of a migration -- it compares the code schema to the live database and applies changes.

    If the command prompts for confirmation, it should auto-accept in non-TTY mode. If it requires interactive input that cannot be suppressed, note it for the checkpoint.

    Verify the push succeeded by checking the command output for the errorMessage column addition.

    **Step 2: Build verification**

    Run `npx next build` to verify the complete application builds without errors. This catches:
    - TypeScript compilation errors across all files
    - Import resolution issues (missing modules, circular deps)
    - Next.js specific errors (invalid route handlers, bad server/client boundaries)

    The build MUST pass. If it fails, fix the issues before proceeding.

    **Step 3: Run full test suite**

    Run `npx vitest run --reporter=verbose` to execute all unit tests created in Plan 01.

    Expected tests:
    - compose-generator.test.ts: template variable substitution, sidecar uncommenting, YAML validity
    - docker-service.test.ts: lifecycle operations with mocked execFile, status derivation from mocked containers

    All tests MUST pass.

    **Step 4: Verify API route structure**

    Run `ls -la src/app/api/environments/` and `ls -la src/app/api/environments/\[id\]/` to confirm all route files exist:
    - src/app/api/environments/route.ts (GET, POST)
    - src/app/api/environments/[id]/route.ts (GET, DELETE)
    - src/app/api/environments/[id]/start/route.ts (POST)
    - src/app/api/environments/[id]/stop/route.ts (POST)

    **Step 5: Verify UI component structure**

    Run `ls -la src/app/dashboard/_components/` to confirm all UI files exist:
    - environment-list.tsx
    - environment-card.tsx
    - create-environment-dialog.tsx
    - delete-environment-dialog.tsx
    - status-badge.tsx
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` completes without error
    - `npx next build` exits 0
    - `npx vitest run` exits 0 with all tests passing
    - src/app/api/environments/route.ts exists
    - src/app/api/environments/[id]/route.ts exists
    - src/app/api/environments/[id]/start/route.ts exists
    - src/app/api/environments/[id]/stop/route.ts exists
    - src/app/dashboard/_components/environment-list.tsx exists
    - src/app/dashboard/_components/environment-card.tsx exists
    - src/app/dashboard/_components/create-environment-dialog.tsx exists
    - src/app/dashboard/_components/delete-environment-dialog.tsx exists
    - src/app/dashboard/_components/status-badge.tsx exists
  </acceptance_criteria>
  <done>Schema pushed to PostgreSQL (errorMessage column exists). Application builds without errors. All unit tests pass. All API route files and UI component files exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard environment lifecycle UI</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
    Human verification of the complete dashboard UI. Claude starts the dev server if not running (`npm run dev`), then the user verifies visually in their browser.
  </action>
  <verify>User confirms the dashboard UI matches expectations</verify>
  <done>User types "approved" confirming the dashboard renders correctly with empty state, creation dialog, and card layout.</done>
  <what-built>
    Complete environment lifecycle: backend Docker service + API routes + dashboard UI with environment cards, creation dialog, delete confirmation, status badges with polling, and loading/empty states. This replaces the Phase 2 dashboard placeholder with a functional environment management interface.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (if not already running)
    2. Open https://devdock.murx.ch/dashboard in your browser (or localhost:3000/dashboard)
    3. Verify you see the empty state: Server icon, "No environments yet" heading, "Create your first development environment to get started." body text, and a "New Environment" button
    4. Click "New Environment" -- verify the dialog opens with:
       - Title: "Create Environment"
       - Name field (required, placeholder "my-project")
       - Git Repository URL field (optional)
       - Separator line
       - "Sidecar Services" section with PostgreSQL and Redis checkboxes
       - "Discard" and "Create & Start" buttons
    5. Try submitting with empty name -- verify error message appears
    6. Click "Discard" -- verify dialog closes

    NOTE: Actually creating an environment requires Docker socket access. If the user `mohed_abbas` is not yet in the docker group, creation will fail with an error status. This is expected -- Docker socket permissions are a VPS setup item. The UI should handle the error gracefully (environment card shows "Error" status badge with error message on hover).

    7. If Docker access works: try creating a test environment with name "test-env" (no repo URL, no sidecars). Verify:
       - Card appears with "Starting" status (amber, pulsing dot)
       - After Docker starts, status transitions to "Running" (green)
       - Stop button appears when running
       - Click Stop -- status transitions to "Stopping" then "Stopped"
       - Start button appears when stopped
       - Delete button opens confirmation dialog with "Delete test-env?"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema push -> Database | Drizzle-kit applies DDL changes to PostgreSQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | drizzle-kit push | accept | Schema push runs from dev machine with direct DB access. Same trust level as existing Phase 1/2 schema pushes. Drizzle-kit only adds columns, does not drop data. |
</threat_model>

<verification>
1. `npx drizzle-kit push` completes successfully
2. `npx next build` exits 0
3. `npx vitest run` exits 0
4. Dashboard renders correctly in browser
5. Empty state displays when no environments exist
6. Creation dialog opens with all fields per UI-SPEC
</verification>

<success_criteria>
- Database schema is synced (errorMessage column exists)
- Application compiles and builds successfully
- All unit tests pass
- Human confirms dashboard UI renders correctly
- Phase 3 delivers all 7 requirements (ENV-01 through ENV-07)
</success_criteria>

<output>
After completion, create `.planning/phases/03-environment-lifecycle/03-04-SUMMARY.md`
</output>
