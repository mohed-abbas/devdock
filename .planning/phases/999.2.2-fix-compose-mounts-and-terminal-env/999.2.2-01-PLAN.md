---
phase: 999.2.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md
autonomous: true
requirements: []
gap_closure: true
security_enforcement: true

must_haves:
  truths:
    - "terminal service resolves DATABASE_URL from env at boot and passes envSchema validation in src/lib/config.ts"
    - ".env.example no longer pre-fills DEVDOCK_DATA_DIR, CLAUDE_CONFIG_PATH, or PRODUCTION_APPS_DIR with relative/commented-template paths; each is a BLANK key guarded by an absolute-path comment block"
    - "bash scripts/stack-smoke.sh exits 0 on a cold run (1st consecutive invocation after docker compose down -v)"
    - "bash scripts/stack-smoke.sh exits 0 on the immediately-following idempotency run (2nd consecutive invocation)"
    - "999.2-VALIDATION.md frontmatter reflects the green terminal state: nyquist_compliant: true, status: approved, gap_closure_phase notes 999.2.2 closure"
    - "999.2-VALIDATION.md Per-Task Verification Map rows 01-T3, 02-T3, 04-T1, 10-T1 show a ✅ marker"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Compose stack definition with terminal service DATABASE_URL env var and unchanged mirror-mount bind-mount pattern (D-09 preserved)"
      contains: "DATABASE_URL: postgresql://${POSTGRES_USER:-devdock}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-devdock}"
    - path: ".env.example"
      provides: "Template with absolute-path-required comment blocks for DEVDOCK_DATA_DIR, CLAUDE_CONFIG_PATH, PRODUCTION_APPS_DIR"
      contains: "DEVDOCK_DATA_DIR="
    - path: ".planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md"
      provides: "Updated row-flips and frontmatter for Phase 999.2 green sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "docker-compose.yml terminal service environment block"
      to: "src/lib/config.ts envSchema.DATABASE_URL"
      via: "env_file: .env loading at container start; validated at module-init via loadConfig()"
      pattern: "DATABASE_URL: postgresql://"
    - from: ".env.example"
      to: "user's .env for compose env_file loading"
      via: "Copy-template workflow: blank host-path vars force explicit local-dev choice before docker compose up"
      pattern: "MUST be an absolute path"
    - from: "scripts/stack-smoke.sh"
      to: "docker-compose.yml + .env"
      via: "Full stack cold-start + healthcheck probe + down -v; re-run validates idempotent teardown"
      pattern: "docker compose .* up"
---

<objective>
Close GAP-4 (compose mount paths) and GAP-5 (terminal service DATABASE_URL), re-run the Phase 999.2 Plan 10 gate sequence twice (idempotency), and flip `999.2-VALIDATION.md` to `nyquist_compliant: true`, `status: approved`.

Purpose: Phase 999.2.1 unblocked the earlier stages of Plan 10's gate and surfaced two residual defects that block `bash scripts/stack-smoke.sh` from exiting 0. Without this plan, Phase 999.2 cannot be signed off.

Output: Two small compose/env edits + a row-and-frontmatter flip in the parent phase's VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/999.2.2-fix-compose-mounts-and-terminal-env/999.2.2-CONTEXT.md
@.planning/phases/999.2.1-fix-entrypoint-test-and-dockerfile-build-env/999.2.1-01-SUMMARY.md
@.planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md
@.planning/phases/999.2-devdock-self-containerization/999.2-10-SUMMARY.md
@.planning/phases/999.2-devdock-self-containerization/999.2-RESEARCH.md
@docker-compose.yml
@.env.example
@src/lib/config.ts
@CLAUDE.md

<interfaces>
<!-- Key contracts. Executor must NOT refactor these; just feed them the env they expect. -->

From src/lib/config.ts (envSchema, lines 4-37):
```typescript
const envSchema = z.object({
  DATABASE_URL: z.string().url().startsWith('postgresql://'),  // REQUIRED — fails at loadConfig() if missing
  AUTH_SECRET: z.string().min(32, ...),                         // REQUIRED
  DEVDOCK_DATA_DIR: z.string().default('./data'),               // schema has default, but compose mount needs absolute host path
  CLAUDE_CONFIG_PATH: z.string().optional().default(''),
  PRODUCTION_APPS_DIR: z.string().optional().default(''),
  // ... other fields
});
// loadConfig() calls envSchema.safeParse(process.env) and throws on failure.
// config is evaluated at MODULE IMPORT TIME (line 53: export const config = loadConfig()).
// terminal-server.ts imports docker-service.ts which imports config.ts transitively.
```

From docker-compose.yml (app service, line 88 — the analog to mirror into terminal):
```yaml
DATABASE_URL: postgresql://${POSTGRES_USER:-devdock}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-devdock}
```

From docker-compose.yml (terminal service environment block, lines 145-153) — DATABASE_URL is MISSING:
```yaml
environment:
  NODE_ENV: production
  DOCKER_SOCKET: /var/run/docker.sock
  DOCKER_GID: ${DOCKER_GID:-}
  TERMINAL_PORT: "3001"
  AUTH_SECRET: ${AUTH_SECRET:?AUTH_SECRET must be set in .env.local}
  AUTH_URL: ${AUTH_URL:-http://localhost:${CADDY_INTERNAL_PORT:-8080}}
  DEVDOCK_URL: ${AUTH_URL:-http://localhost:${CADDY_INTERNAL_PORT:-8080}}
  CLAUDE_CONFIG_PATH: ${CLAUDE_CONFIG_PATH:-}
# DATABASE_URL must be inserted here (single line, mirroring app line 88)
```

From docker-compose.yml (app service bind-mounts, lines 113-117) — MIRROR-MOUNT PATTERN, DO NOT CHANGE:
```yaml
- ${DEVDOCK_DATA_DIR:-/srv/devdock/data}:${DEVDOCK_DATA_DIR:-/srv/devdock/data}
- ${PRODUCTION_APPS_DIR:-/srv/devdock/apps-empty}:${PRODUCTION_APPS_DIR:-/srv/devdock/apps-empty}:ro
- ${CLAUDE_CONFIG_PATH:-/srv/devdock/claude-empty}:${CLAUDE_CONFIG_PATH:-/srv/devdock/claude-empty}:ro
```
D-09 mirror-mount is LOCKED. Fix goes on the `.env.example` side (documentation) only.

From .env.example (current state, relevant lines):
- Line 23-24:  `# Where per-environment data dirs and generated docker-compose.yml files live.`
               `DEVDOCK_DATA_DIR=./data`    <-- CHANGE: blank value + absolute-path comment
- Line 32-34:  `# Optional: host path containing a .claude/ directory ...`
               `# CLAUDE_CONFIG_PATH=/home/you/.claude`    <-- CHANGE: absolute-path comment wording
- Line 63-66:  `# -------- Production apps monitoring (optional) ------ ...`
               `# PRODUCTION_APPS_DIR=/home/murx`    <-- CHANGE: absolute-path comment wording
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply compose-side and env-template fixes (GAP-4 + GAP-5)</name>
  <files>docker-compose.yml, .env.example</files>
  <read_first>
    - docker-compose.yml (full file — know the surrounding context for the terminal `environment:` block at lines 145-153 and the mirror-mount block at lines 113-117)
    - .env.example (full file — understand section headers at lines 6, 17, 21, 39, 50, 63)
    - .planning/phases/999.2.2-fix-compose-mounts-and-terminal-env/999.2.2-CONTEXT.md (D-01..D-07, D-11 binding decisions)
    - .planning/phases/999.2.1-fix-entrypoint-test-and-dockerfile-build-env/999.2.1-01-SUMMARY.md (§ "New Gaps Surfaced" — verbatim failure output for GAP-4 and GAP-5)
    - .planning/phases/999.2-devdock-self-containerization/999.2-RESEARCH.md (D-09 mirror-mount rationale — explains why the fix stays on the `.env.example` side, not compose)
    - src/lib/config.ts (envSchema — confirms DATABASE_URL is REQUIRED at module init, transitively imported by terminal-server.ts)
  </read_first>
  <action>
    Two concrete edits — keep them atomic (single commit or two sibling commits; both acceptable per CONTEXT Claude's Discretion).

    **Edit A — docker-compose.yml (GAP-5 fix):**
    In the `terminal` service `environment:` block (currently lines 145-153), insert this line immediately after `NODE_ENV: production` (so it sits alongside the other env vars in the same visual position as the `app` service line 88). Use exactly this string (copy verbatim, including the `${POSTGRES_USER:-devdock}` default-expansion and the `${POSTGRES_PASSWORD}` no-default form — mirrors app service line 88 byte-for-byte):

    ```yaml
          DATABASE_URL: postgresql://${POSTGRES_USER:-devdock}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-devdock}
    ```

    The 6-space indent matches the existing env keys under `terminal.environment`. Do NOT add `POSTGRES_HOST`, `POSTGRES_USER`, or `POSTGRES_DB` as separate keys to the terminal env (terminal does not use them at runtime; only DATABASE_URL is required by loadConfig() — per D-06). Do NOT change the mirror-mount block at lines 113-117 or 156 — D-09 is locked (per D-01).

    **Edit B — .env.example (GAP-4 fix, three host-path vars):**

    Replace the current three host-path locations with blank-value + absolute-path comment blocks. Exact replacements (planner's discretion on wording within clarity constraint per D-11):

    1. **DEVDOCK_DATA_DIR** — replace current lines 22-24:
       ```
       # Where per-environment data dirs and generated docker-compose.yml files live.
       DEVDOCK_DATA_DIR=./data
       ```
       with:
       ```
       # Where per-environment data dirs and generated docker-compose.yml files live.
       # MUST be an absolute path on the host. Docker bind-mounts reject relative
       # paths, and the compose mirror-mount pattern ${VAR}:${VAR} (D-09) requires
       # the same absolute path on both sides. Leave blank to fall back to the
       # compose default /srv/devdock/data (prod VPS layout).
       # Local dev example: /Users/you/devdock-data
       DEVDOCK_DATA_DIR=
       ```

    2. **CLAUDE_CONFIG_PATH** — replace current lines 32-34:
       ```
       # Optional: host path containing a `.claude/` directory to mount into dev
       # containers so Claude Code CLI inherits your login.
       # CLAUDE_CONFIG_PATH=/home/you/.claude
       ```
       with:
       ```
       # Optional: host path containing a `.claude/` directory to mount into dev
       # containers so Claude Code CLI inherits your login.
       # MUST be an absolute path on the host (Docker bind-mount requirement,
       # mirror-mount pattern D-12). Leave commented to fall back to the compose
       # default /srv/devdock/claude-empty.
       # CLAUDE_CONFIG_PATH=/home/you/.claude
       ```

    3. **PRODUCTION_APPS_DIR** — replace current lines 63-66:
       ```
       # -------- Production apps monitoring (optional) ------------------------------
       # Directory containing your prod app docker-compose stacks to surface on the
       # dashboard. Leave blank to hide the Production Apps section.
       # PRODUCTION_APPS_DIR=/home/murx
       ```
       with:
       ```
       # -------- Production apps monitoring (optional) ------------------------------
       # Directory containing your prod app docker-compose stacks to surface on the
       # dashboard. MUST be an absolute path on the host (Docker bind-mount
       # requirement). Leave commented to fall back to the compose default
       # /srv/devdock/apps-empty. Leave blank to hide the Production Apps section.
       # PRODUCTION_APPS_DIR=/home/murx
       ```

    **Out of scope (do NOT touch, per D-11):**
    - The duplicated `DATABASE_URL` at line 9 vs commented alternative at line 77.
    - Any section reordering.
    - Any other comment rewording.
    - `docker-compose.override.yml`.
    - `src/lib/config.ts`.
    - `tests/compose-lint.sh` (optional absolute-path assertion flagged as deferred in D-11 Claude's Discretion — skip unless trivially cheap, and if added, out of scope of this task; do it in a follow-up commit only if it's a 3-line grep).

    Commit convention: `fix(999.2.2): ...` subject style per 999.2.1 precedent. Two commits (one per gap) or a single combined commit both acceptable.
  </action>
  <verify>
    <automated>grep -nE '^\s*DATABASE_URL:\s*postgresql://\$\{POSTGRES_USER:-devdock\}' docker-compose.yml | wc -l | grep -q '^\s*2\s*$' && grep -nE '^DEVDOCK_DATA_DIR=\s*$' .env.example && grep -iE 'absolute path' .env.example | wc -l | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && docker compose config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^\s*DATABASE_URL:\s*postgresql://' docker-compose.yml` returns exactly **2** matches (app line 88 unchanged + new terminal line).
    - `grep -c '^\s*DATABASE_URL:' docker-compose.yml` equals **2**.
    - The terminal service environment block (lines 145-160 range) contains a `DATABASE_URL:` line when inspected.
    - `grep -nE '^DEVDOCK_DATA_DIR=\s*$' .env.example` returns **1** match (blank value on its own line — no `./data`, no quoted empty).
    - `grep -c -iE 'absolute path' .env.example` returns **>= 3** (one near each of DEVDOCK_DATA_DIR, CLAUDE_CONFIG_PATH, PRODUCTION_APPS_DIR).
    - `grep -nE '^DEVDOCK_DATA_DIR=\./data' .env.example` returns **0 matches** (the old relative value is gone).
    - `docker compose config --quiet` exits **0** (compose still parses; no syntax regression).
    - `grep -nE '^\s*- \$\{DEVDOCK_DATA_DIR:-/srv/devdock/data\}:\$\{DEVDOCK_DATA_DIR:-/srv/devdock/data\}' docker-compose.yml` returns **1** match (D-09 mirror-mount block preserved unchanged).
    - No changes outside `docker-compose.yml` and `.env.example` in this task (verify with `git status --short` — only those two paths should appear; VALIDATION.md flip is a later task).
  </acceptance_criteria>
  <done>
    Both files edited per the above; `docker compose config --quiet` passes; D-09 mirror-mount pattern preserved verbatim; no other files modified in this task's commits; commit(s) use `fix(999.2.2): ...` subject style.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run Phase 999.2 Plan 10 gate sequence (×2 for idempotency)</name>
  <files>(no file edits — this task runs validation commands only)</files>
  <read_first>
    - .planning/phases/999.2-devdock-self-containerization/999.2-10-SUMMARY.md (Plan 10 gate sequence definition — the exact commands to run, in order)
    - .planning/phases/999.2.2-fix-compose-mounts-and-terminal-env/999.2.2-CONTEXT.md (D-08 — gate sequence locked)
    - .planning/phases/999.2.1-fix-entrypoint-test-and-dockerfile-build-env/999.2.1-01-SUMMARY.md (gate-result table at "Plan 10 Gate Results" — baseline state: tsc/vitest/compose-lint already green from 999.2.1)
    - scripts/stack-smoke.sh (to understand what cold-start + teardown looks like; do NOT modify)
    - tests/entrypoint.test.sh (to confirm the GNU-stat / gosu reliance that justifies the Darwin-skip — do NOT modify)
    - tests/compose-lint.sh (do NOT modify)
  </read_first>
  <action>
    Run the full Plan 10 gate sequence in the exact order below. Capture stdout + exit code for each step. Stop on first non-zero exit code and surface the failure.

    **Preconditions:**
    - Working tree clean except for any pending unpushed `fix(999.2.2): ...` commits from Task 1.
    - User's `.env` has the three absolute host paths set locally (out of planning scope; the author already has this from the 999.2.1 local workaround — per CONTEXT `<deferred>`).
    - Docker Desktop running.

    **Step 1 — TypeScript:**
    ```
    npx tsc --noEmit
    ```
    Expected: exit 0, no output. Already green in 999.2.1 per SUMMARY; this is a regression check.

    **Step 2 — Vitest:**
    ```
    npx vitest run
    ```
    Expected: exit 0, `Tests  82 passed (82)` or higher (phase 999.2.1 baseline was 82/82). Already green; regression check.

    **Step 3 — Compose lint:**
    ```
    bash tests/compose-lint.sh
    ```
    Expected: exit 0, trailing line `compose-lint: PASS`. Already green; regression check.

    **Step 4 — Entrypoint integration test (Darwin-skip expected):**
    ```
    bash tests/entrypoint.test.sh
    ```
    Expected on Darwin host: the test is Darwin-skipped per 999.2.1 decision (entrypoint script uses GNU `stat -c` + `gosu`; neither exists on macOS). If the script exits non-zero because of the Darwin-specific shape issue — document it in the SUMMARY under "Darwin skip (per D-08, validated via stack-smoke)" and proceed. Do NOT patch the test in this phase. If the script has already been hardened for Darwin skip (exit 0 with a "skipped" log), note so and proceed.

    **Step 5 — Stack smoke (cold):**
    ```
    bash scripts/stack-smoke.sh
    ```
    This is the first consecutive invocation. Expected: exit 0. The script boots the full compose stack, probes healthchecks for postgres/caddy/app/terminal, then tears down with `docker compose down -v`. If GAP-4 or GAP-5 regresses, this will fail with the symptoms documented in `999.2.1-01-SUMMARY.md` GAP-4 / GAP-5 sections — re-inspect Task 1 edits before debugging elsewhere.

    **Step 6 — Stack smoke (idempotency):**
    ```
    bash scripts/stack-smoke.sh
    ```
    Second consecutive invocation. Expected: exit 0. Validates that the previous `down -v` teardown left no residue (no orphaned containers, no stale networks, no volume lock contention).

    **On any non-zero exit:**
    - Capture the failure output verbatim.
    - Do NOT proceed to Task 3.
    - Surface to user with the specific failing step + the exact command reproducer.
    - If GAP-4 / GAP-5 regressed, the fix is in Task 1's `docker-compose.yml` / `.env.example`, not in the scripts.

    **On all steps green:**
    - Record a one-line summary per step (command, exit code, duration if easy, top-of-output if relevant).
    - Proceed to Task 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && bash tests/compose-lint.sh && (bash tests/entrypoint.test.sh || echo "Darwin skip acknowledged per D-08") && bash scripts/stack-smoke.sh && bash scripts/stack-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits **0**, no diagnostic output.
    - `npx vitest run` exits **0** and reports ≥ 82 passing tests (999.2.1 baseline).
    - `bash tests/compose-lint.sh` exits **0**, final line is `compose-lint: PASS`.
    - `bash tests/entrypoint.test.sh` either exits 0 **OR** is explicitly Darwin-skipped per D-08; the skip is documented in the task's execution log (not silently ignored).
    - `bash scripts/stack-smoke.sh` exits **0** on the 1st run (cold start).
    - `bash scripts/stack-smoke.sh` exits **0** on the immediately-following 2nd run (idempotency).
    - No docker containers in state `unhealthy` at end of either smoke run (`docker ps --filter name=devdock- --format '{{.Names}} {{.Status}}'` shows either empty or only `healthy`/`Up` statuses — the scripts tear down on exit, so empty is expected).
    - No changes committed in this task (validation-only; the VALIDATION.md flip belongs to Task 3).
  </acceptance_criteria>
  <done>
    All six gate steps exit 0 (with Step 4 allowed to be Darwin-skipped per D-08). Execution log captured for Task 3 to reference. No file mutations in this task.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip 999.2-VALIDATION.md row status and frontmatter to approved</name>
  <files>.planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md</files>
  <read_first>
    - .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md (current frontmatter lines 1-10 + Per-Task Verification Map rows 45, 48, 51, 66 + Validation Sign-Off section lines 110-119)
    - .planning/phases/999.2.2-fix-compose-mounts-and-terminal-env/999.2.2-CONTEXT.md (D-09, D-10 — binding row/frontmatter flip scope)
    - Task 2 execution log (the gate-result summaries just captured — this task records them for audit trail)
  </read_first>
  <action>
    Gate: Only run this task if **all of Task 2's six gates exited 0** (with Step 4's Darwin-skip allowed per D-08). If Task 2 failed, stop; this task does not run.

    **Edit 1 — Frontmatter (lines 1-10 of `.planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md`):**

    Current frontmatter:
    ```yaml
    ---
    phase: 999.2
    slug: devdock-self-containerization
    status: gaps-found
    nyquist_compliant: false
    wave_0_complete: true
    created: 2026-04-18
    updated: 2026-04-19
    gap_closure_phase: [999.2.1 (partial: GAPs 1-3 closed), 999.2.2 (pending: GAPs 4-5)]
    ---
    ```

    Replace with:
    ```yaml
    ---
    phase: 999.2
    slug: devdock-self-containerization
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-18
    updated: 2026-04-19
    gap_closure_phase: [999.2.1 (closed: GAPs 1-3), 999.2.2 (closed: GAPs 4-5)]
    ---
    ```

    (Keep `updated: 2026-04-19` as-is; today's date is already 2026-04-19 per env. If the executor runs on a later date, update to that date.)

    **Edit 2 — Per-Task Verification Map (table rows in section "## Per-Task Verification Map"):**

    Flip the Status column to ✅ green for the four rows listed below. Leave all other rows untouched.

    | Row ID | Current Status (pre-flip) | New Status (post-flip) |
    |--------|---------------------------|------------------------|
    | 01-T3  | `❌ red (gap-closure 999.2.1)` | `✅ green (after 999.2.2 stack-smoke × 2)` |
    | 02-T3  | `⚠️ E2E blocked by 01-T3 gap` | `✅ green (after 999.2.2 stack-smoke × 2)` |
    | 04-T1  | `❌ red — pg_isready hostname mismatch on host (gap-closure 999.2.1)` | `✅ green (after 999.2.2 — Darwin-skip acknowledged; containerized gate via stack-smoke)` |
    | 10-T1  | `❌ red — gated by 03-T1 + 04-T1 gaps` | `✅ green (after 999.2.2 stack-smoke × 2)` |

    **Edit 3 — Validation Sign-Off section (lines 110-119 range):**

    Check the now-satisfied checkbox:
    ```
    - [x] `nyquist_compliant: true` — **APPROVED: 999.2.1 closed GAPs 1-3; 999.2.2 closed GAPs 4-5 (compose mount path policy + terminal DATABASE_URL). Full stack-smoke × 2 green end-to-end.**
    ```
    (Replaces the existing `- [ ]` line and its "PARTIAL:" text verbatim.)

    Append a new approval line **below** the existing "Approval: gaps-found 2026-04-19 — ..." paragraph (do not delete the historical one — preserve audit trail):
    ```
    **Approval:** approved 2026-04-19 — 999.2.2 closed GAP-4 (compose mount path policy via `.env.example` absolute-path comment blocks) and GAP-5 (terminal service DATABASE_URL wiring). Full Plan 10 gate sequence re-ran green: tsc ✅, vitest ≥82/82 ✅, compose-lint ✅, entrypoint.test Darwin-skip acknowledged per D-08, stack-smoke.sh ×2 ✅✅. Rows 01-T3, 02-T3, 04-T1, 10-T1 flipped to ✅ green. Phase 999.2 sign-off complete.
    ```

    **Commit:**
    ```
    docs(999.2.2): flip 999.2-VALIDATION.md rows 01-T3/02-T3/04-T1/10-T1 and frontmatter to approved
    ```

    **Out of scope:**
    - Do NOT edit any other VALIDATION.md row (05-*, 06-*, 07-*, 08-*, 09-*, 03-*).
    - Do NOT edit the "Gaps Discovered During Plan 10 E2E Validation" table (it's historical; leave it as-is — the frontmatter + sign-off sections capture the resolution).
    - Do NOT edit any Phase 999.2.2 SUMMARY files in this task (SUMMARY generation is part of the execute-plan workflow, not this plan's task list).
  </action>
  <verify>
    <automated>grep -q '^status: approved$' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md && grep -q '^nyquist_compliant: true$' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md && grep -E '\| 01-T3 \|.*✅' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md && grep -E '\| 02-T3 \|.*✅' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md && grep -E '\| 04-T1 \|.*✅' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md && grep -E '\| 10-T1 \|.*✅' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md && grep -q 'Approval:\*\* approved 2026-04' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^status: approved$' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md` returns **1**.
    - `grep -c '^nyquist_compliant: true$' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md` returns **1**.
    - `grep -c '^status: gaps-found$' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md` returns **0** (old status removed).
    - `grep -c '^nyquist_compliant: false$' .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md` returns **0** (old flag removed).
    - The row for **01-T3** in the Per-Task Verification Map contains `✅` in its Status column.
    - The row for **02-T3** contains `✅`.
    - The row for **04-T1** contains `✅`.
    - The row for **10-T1** contains `✅`.
    - A new `**Approval:** approved 2026-04-...` line exists in the Validation Sign-Off section, BELOW the original `**Approval:** gaps-found 2026-04-19 — ...` line (both preserved).
    - The checkbox `- [x] \`nyquist_compliant: true\`` (checked, not unchecked) is present in the Validation Sign-Off section.
    - `git diff --stat .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md` shows ONLY this file changed in this task's commit (no scope creep into other rows or files).
    - `gap_closure_phase` frontmatter entry reads `[999.2.1 (closed: GAPs 1-3), 999.2.2 (closed: GAPs 4-5)]`.
  </acceptance_criteria>
  <done>
    VALIDATION.md frontmatter, the four target rows, and the Validation Sign-Off section all reflect the post-999.2.2 approved state. Commit landed. No other rows or files touched in this commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.example` → public git history | Template file committed to repo; visible to anyone with repo read access. Must contain zero real secrets. |
| `.env` (gitignored) → terminal container env | Real secrets injected at `docker compose up` via implicit `env_file: .env` loading. Container-internal only. |
| `devdock-net` internal Docker network → postgres service | Terminal service gains DATABASE_URL; needs network reachability to postgres:5432. Network is isolated (no published ports per T-999.2-04). |
| terminal container → host `/var/run/docker.sock` | Unchanged in this phase; existing privileged bind-mount documented in INFRA-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.2.2-01 | Information Disclosure | `.env.example` committed with real credentials | mitigate | Task 1 Edit B writes ONLY comment blocks and a blank-value key; no passwords, no tokens, no hostnames of real hosts. Executor's acceptance_criteria greps confirm `DEVDOCK_DATA_DIR=` is blank (not `=/Users/murx/...`). Code review: reject any non-blank or non-comment value in the three changed locations. |
| T-999.2.2-02 | Information Disclosure | `.env.example` absolute-path comment leaking host layout | accept | Comments use generic placeholders (`/Users/you/...`, `/home/you/...`). No real usernames, no prod server paths beyond `/srv/devdock/*` which were already public per 999.2-RESEARCH.md. Risk: trivial (layout conventions are industry-standard). |
| T-999.2.2-03 | Elevation of Privilege | Terminal container gains DATABASE_URL ⇒ DB write capability | mitigate | Terminal already reached postgres at network layer (both on `devdock-net`, no port filter between them). The change adds ENV-level exposure of the credential string, not a new network path. DATABASE_URL uses the same `${POSTGRES_USER:-devdock}` role as the app — no privilege escalation vs app. Terminal's code (`server/terminal-server.ts`) does not execute DB queries; `loadConfig()` only validates. If a future terminal-side code path ever touches the DB, the threat model must be revisited (flagged via D-07 deferred config-splitting idea). Docker socket remains the primary privilege surface (T-999.2-01 mitigation unchanged). |
| T-999.2.2-04 | Tampering | POSTGRES_PASSWORD interpolation exposes credential in container ENV | accept (pre-existing) | Same exposure as app service line 88 — credential is already in container ENV for `app`. Adding it to `terminal` does not widen the blast radius. Container ENV is visible to `docker inspect` / root on host; Docker socket access already implies same-or-greater capability (T-999.2-01). No net change. |
| T-999.2.2-05 | Denial of Service | `.env.example` blank `DEVDOCK_DATA_DIR=` causes silent fall-through to `/srv/devdock/data` default | mitigate | The comment block above the blank key explicitly instructs the reader on the absolute-path requirement and the /srv/devdock/* fallback. `scripts/stack-smoke.sh` exercises both cold + idempotent paths, so a missing absolute-path override fails loudly at mount time (observable via the smoke gate in Task 2), not silently at runtime. Local-dev writers who copy `.env.example` → `.env` without reading will hit the prod-path fallback and get a clear "mounts denied" error — failure is loud, not silent. |
| T-999.2.2-06 | Tampering | VALIDATION.md row-flip could mask unfinished work | mitigate | Task 3 is gated on Task 2's six-step gate ALL exiting 0. Acceptance criteria explicitly grep for ✅ on the four target rows AND preserve the historical `gaps-found` approval line (audit trail). Reviewer can trivially verify Task 2's execution log before accepting Task 3's commit. |
</threat_model>

<verification>
Phase gate:
1. `docker compose config --quiet` exits 0 (compose still valid).
2. `grep -c '^\s*DATABASE_URL:' docker-compose.yml` equals 2.
3. `grep -c -iE 'absolute path' .env.example` returns >= 3.
4. `bash scripts/stack-smoke.sh && bash scripts/stack-smoke.sh` — both exit 0.
5. `.planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md` frontmatter contains `nyquist_compliant: true` and `status: approved`.
6. The four VALIDATION.md rows (01-T3, 02-T3, 04-T1, 10-T1) all show ✅ markers.
7. D-09 mirror-mount pattern (lines 113-117 of docker-compose.yml) is byte-for-byte unchanged.
8. No edits outside the three files declared in `files_modified`.
</verification>

<success_criteria>
- [ ] docker-compose.yml terminal service has exactly one new line: `DATABASE_URL: postgresql://${POSTGRES_USER:-devdock}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-devdock}`
- [ ] .env.example has three absolute-path comment blocks (DEVDOCK_DATA_DIR, CLAUDE_CONFIG_PATH, PRODUCTION_APPS_DIR), each mentioning "absolute path"
- [ ] `DEVDOCK_DATA_DIR=./data` no longer exists in .env.example
- [ ] `bash scripts/stack-smoke.sh` exits 0 twice in a row
- [ ] `999.2-VALIDATION.md` frontmatter: `status: approved`, `nyquist_compliant: true`
- [ ] `999.2-VALIDATION.md` rows 01-T3, 02-T3, 04-T1, 10-T1 all contain ✅
- [ ] No source-code (src/, server/, scripts/) files modified in this phase
- [ ] D-09 mirror-mount pattern in docker-compose.yml preserved verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/999.2.2-fix-compose-mounts-and-terminal-env/999.2.2-01-SUMMARY.md` covering: file patches applied (Task 1), full gate-run log with exit codes and test counts (Task 2), VALIDATION.md diff (Task 3), and frontmatter `nyquist_compliant: true`, `status: complete`.
</output>
