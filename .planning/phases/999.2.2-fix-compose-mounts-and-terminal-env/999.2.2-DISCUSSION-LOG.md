# Phase 999.2.2: Fix compose mount paths and terminal env — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 999.2.2-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 999.2.2-fix-compose-mounts-and-terminal-env
**Areas discussed:** Mount path policy (GAP-4), Terminal config approach (GAP-5), .env.example cleanup

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Mount path policy (GAP-4) | ${VAR}:${VAR} mounts break on local dev (Darwin) while preserving D-09 | ✓ |
| Terminal config approach (GAP-5) | Terminal service missing DATABASE_URL; fix at compose vs config.ts level | ✓ |
| .env.example cleanup | Hygiene on .env.example beyond GAP-4-forced changes | ✓ |

**User's choice:** All three selected.

---

## GAP-4 — Mount path policy

### Q1: Fix approach

| Option | Description | Selected |
|--------|-------------|----------|
| A1: Fix .env.example defaults to absolute | No code/compose change; update .env.example defaults + doc note. D-09 preserved. | ✓ |
| A1b: Dev-only override for mounts | Keep prod compose mirror-mount; redefine dev mounts in docker-compose.override.yml. Larger diff. | |
| A2: Break D-09 (fixed container paths) | Change compose to ${VAR}:/app/data + introduce DEVDOCK_DATA_DIR_HOST. Touches compose generator. Flagged. | |

**User's choice:** A1 — Fix .env.example defaults to absolute (Recommended).
**Notes:** Tightest gap-closure scope. Preserves D-09 mirror-mount pattern from Phase 999.2.

### Q2: Absolute-path default in .env.example

| Option | Description | Selected |
|--------|-------------|----------|
| /srv/devdock/* (prod-style, commented) | Ship prod-style defaults in .env.example. | |
| Blank with explicit "must set absolute" note | Leave value blank; prominent comment block explains absolute-path requirement. | ✓ |
| ${HOME}/devdock-data style | Shell-expansion-style example. Compose may not expand ${HOME} in env_file. Flagged. | |

**User's choice:** Blank with explicit "must set absolute" note.
**Notes:** Forces conscious choice on local dev; no implicit /srv/devdock assumption on dev machines.

### Q3: docker-compose.yml fallbacks

| Option | Description | Selected |
|--------|-------------|----------|
| Keep /srv/devdock/* fallbacks | Current ${VAR:-/srv/devdock/...} stays. Matches author's prod VPS. | ✓ |
| Remove fallbacks — require explicit set | ${VAR:?must be set} fails loudly. Prevents silent-failure pattern. | |
| Fallback to a dev-friendly path | e.g. ${VAR:-./data}. Reintroduces GAP-4 root cause. Flagged. | |

**User's choice:** Keep /srv/devdock/* fallbacks.
**Notes:** Combined with blank .env.example, local dev forces explicit absolute path.

---

## GAP-5 — Terminal config approach

### Q1: Fix approach

| Option | Description | Selected |
|--------|-------------|----------|
| B1: Add DATABASE_URL to terminal env | One-line mirror of app service line 88 in compose. Smallest. Preserves single-source config.ts. | ✓ |
| B2: Split config.ts — terminal-only validator | Create src/lib/config-terminal.ts; refactor docker-service.ts import. Cleaner but scope creep. | |
| B3: Make DATABASE_URL optional in zod schema | Weakens the config.ts guarantee; app could boot without DB. Flagged. | |

**User's choice:** B1 — Add DATABASE_URL to terminal env (Recommended).
**Notes:** 999.2.1 SUMMARY pre-biased toward this as "the smaller option." Preserves single-source config discipline.

### Q2: Scope of terminal env additions

| Option | Description | Selected |
|--------|-------------|----------|
| Only DATABASE_URL | Minimum diff; terminal doesn't use Postgres at runtime. | ✓ |
| Match app service's full DB env block | Also add POSTGRES_HOST / POSTGRES_USER / POSTGRES_DB. Over-provisioning. | |

**User's choice:** Only DATABASE_URL.
**Notes:** Gap-closure discipline. No unused env vars added.

---

## .env.example cleanup

### Q1: Scope of cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| C1: GAP-4 + host-path doc only | Tightest scope. Only the three host-path vars touched. | ✓ |
| C2: Also collapse DATABASE_URL duplication | Swap primary DATABASE_URL to @postgres:5432; comment localhost variant. Touches stale-post-999.2 artifact. | |
| C3: No cleanup | Leave DEVDOCK_DATA_DIR=./data; reintroduces GAP-4 on next fresh clone. Contradicts GAP-4 decision. Flagged. | |

**User's choice:** C1 — GAP-4 + host-path doc only.
**Notes:** Tightest gap-closure scope. DATABASE_URL duplication deferred to a future config/docs phase.

---

## Claude's Discretion

- Exact wording of the absolute-path comment blocks in `.env.example`.
- Whether to add a trivial absolute-path assertion to `tests/compose-lint.sh` (nice-to-have, only if cheap).
- Commit sequencing within the plan (separate per gap vs combined).
- Commit message wording.

## Deferred Ideas

- Full `.env.example` reorganization (C2/C3 material) — defer to a dedicated config/docs phase.
- `config.ts` splitting (B2 material) — defer unless a future phase introduces meaningfully divergent terminal config.
- Pre-flight absolute-path validator — flagged as trivial-if-cheap; may be deferred during planning.
- Cleaning up the author's local `.env` workaround paths — gitignored, not a planning concern.
