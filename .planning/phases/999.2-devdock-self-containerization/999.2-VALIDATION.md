---
phase: 999.2
slug: devdock-self-containerization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-18
---

# Phase 999.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (if present) + shell-based stack smoke via `docker compose`; framework TBD by Wave 0 if not present |
| **Config file** | `vitest.config.ts` (if present) — else Wave 0 installs minimal smoke harness |
| **Quick run command** | `npm run lint && npx tsc --noEmit` |
| **Full suite command** | `docker compose -f docker-compose.yml up -d --build && ./scripts/stack-smoke.sh && docker compose down` |
| **Estimated runtime** | ~120 seconds (full stack smoke) |

---

## Sampling Rate

- **After every task commit:** Run `{quick run command}` (typecheck + lint; no container work)
- **After every plan wave:** Run the stack smoke (`docker compose up --build` + probe all services) once Caddy/postgres/app/terminal are introduced
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15s for quick; 120s for stack smoke

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD — populated by planner per task | — | — | — | — | — | — | — | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/stack-smoke.sh` — probes `http://localhost:${CADDY_INTERNAL_PORT}` → 200, `docker exec devdock-app pg_isready -h postgres` → 0, `curl http://caddy:2019/config/` from `app` service → 200, `curl http://app:3000/api/health` → 200, `curl http://terminal:3001/health` → 200
- [ ] `tests/compose-lint.sh` — `docker compose config` exits 0 for both base + override
- [ ] `tests/entrypoint.test.sh` — runs the entrypoint script against a throwaway postgres; asserts drizzle push idempotency and admin seed guard (no duplicate user on re-run)
- [ ] `tests/hash-password.test.ts` — asserts generated bcrypt verifies with bcrypt.compare
- [ ] vitest install if not present (planner checks package.json first)

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live VPS cutover | N/A (phase scope bounds) | Out of scope per CONTEXT `<domain>` | See `HUMAN-UAT.md` runbook |
| Host nginx → DevDock Caddy loopback routing | INFRA-01 (runtime) | Requires real host nginx config + DNS | Documented in HUMAN-UAT.md |
| TLS termination by host nginx (wildcard cert) | INFRA-01 (runtime) | Certbot workflow is host-level | HUMAN-UAT.md references existing cert |
| Claude Code CLI runs from `:/home/dev/.claude:ro` mirror-mount | TERM-03 | Requires actual Claude config on host | HUMAN-UAT smoke: exec into user-project container, run `claude --version` |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (stack-smoke, compose-lint, entrypoint.test, hash-password.test)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter after planner fills per-task map

**Approval:** pending
