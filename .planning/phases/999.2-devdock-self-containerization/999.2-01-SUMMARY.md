---
phase: "999.2"
plan: "01"
subsystem: "test-harness"
tags: ["tdd", "red-stubs", "vitest", "shell-tests", "validation-infrastructure"]
dependency_graph:
  requires: []
  provides:
    - "tests/hash-password.test.ts (RED — driven GREEN by Plan 02)"
    - "tests/compose-lint.sh (RED — driven GREEN by Plan 05)"
    - "tests/entrypoint.test.sh (RED — driven GREEN by Plan 04)"
    - "scripts/stack-smoke.sh (RED — driven GREEN by Plan 10)"
    - "vitest.config.ts updated to discover tests/ directory"
  affects:
    - "All downstream waves in Phase 999.2 (Nyquist validation contract)"
tech_stack:
  added: []
  patterns:
    - "RED stub pattern: fail immediately with explicit FAIL message naming the future plan"
    - "set -euo pipefail + ISO-8601 timestamp echo for all shell tests"
key_files:
  created:
    - "tests/hash-password.test.ts"
    - "tests/compose-lint.sh"
    - "tests/entrypoint.test.sh"
    - "scripts/stack-smoke.sh"
  modified:
    - "vitest.config.ts"
decisions:
  - "Merged vitest.config.ts (add tests/** glob) rather than overwriting to preserve existing src/** and server/** globs and globals:true setting"
  - "Dynamic import used in hash-password.test.ts (await import) rather than static import — correct per plan action; acceptance criteria literal 'from ...' is a shorthand that does not match the dynamic import syntax"
metrics:
  duration: "~6min"
  completed: "2026-04-18"
  tasks_completed: 3
  files_created: 4
  files_modified: 1
---

# Phase 999.2 Plan 01: Wave 0 Test Harness Summary

**One-liner:** Four deliberately-failing RED stubs (vitest + shell) that enforce Nyquist validation contract before implementation begins — each stub names the exact downstream plan that must drive it GREEN.

## What Was Built

Wave 0 validation infrastructure: five files that create a verifiable RED baseline so all subsequent plans' RED→GREEN transitions are observable.

### Stubs Created and Their Downstream Plans

| File | Type | Fails With | Driven GREEN By |
|------|------|-----------|-----------------|
| `tests/hash-password.test.ts` | vitest | `Cannot find module '/src/scripts/hash-password'` | Plan 02 (hash-password helper) |
| `tests/compose-lint.sh` | shell | `FAIL — docker-compose.yml not found` | Plan 05 (compose files) |
| `tests/entrypoint.test.sh` | shell | `FAIL — scripts/entrypoint-app.sh not found` | Plan 04 (entrypoint script) |
| `scripts/stack-smoke.sh` | shell | `FAIL — docker-compose.yml not found` | Plan 10 (full stack gate) |

### vitest.config.ts Updated

Added `tests/**/*.{test,spec}.ts` to the `include` array so vitest discovers both the new `tests/hash-password.test.ts` and the pre-existing `tests/auth/rate-limit.test.ts`.

## RED State Confirmed

All four stubs fail with their expected deliberate RED messages:

1. `npx vitest run tests/hash-password.test.ts` — exits non-zero: `Cannot find module '/src/scripts/hash-password'`
2. `bash tests/compose-lint.sh` — exits non-zero: `FAIL — docker-compose.yml not found (Plan 05 must create it)`
3. `bash tests/entrypoint.test.sh` — exits non-zero: `FAIL — scripts/entrypoint-app.sh not found or not executable (Plan 04 must create it)`
4. `bash scripts/stack-smoke.sh` — exits non-zero: `FAIL — docker-compose.yml not found (Plan 05 must create it)`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] vitest.config.ts already existed — merged instead of overwriting**

- **Found during:** Task 1
- **Issue:** Plan interfaces comment stated "No vitest.config.ts currently exists at repo root." In reality, `vitest.config.ts` existed with `globals: true` and three include globs (`src/**/__tests__/**/*.test.ts`, `src/**/*.test.ts`, `server/**/*.test.ts`). Overwriting verbatim would have dropped `server/**/*.test.ts` discovery and flipped `globals: true → false`, silently breaking existing test coverage.
- **Fix:** Merged the new `tests/**/*.{test,spec}.ts` glob into the existing include array, preserving all prior globs and `globals: true`. The `@` alias was already present. All plan acceptance criteria (`'tests/**/*.{test,spec}.ts'` literal and `'@': path.resolve(__dirname, './src')` literal) are still satisfied.
- **Files modified:** `vitest.config.ts`
- **Commit:** 969a997

**2. [Minor] Acceptance criteria literal 'from ../src/scripts/hash-password' does not match dynamic import**

- **Found during:** Task 1 verification
- **Issue:** The plan's acceptance criteria states the file must contain literal `from '../src/scripts/hash-password'`. The plan's own action text specifies a dynamic `await import('../src/scripts/hash-password')` — not a static `from` import. The path string `../src/scripts/hash-password` is present; the `from` keyword is not applicable to dynamic imports.
- **Fix:** No code change — the implementation correctly matches the plan action (dynamic import). Documented as acceptance criteria shorthand discrepancy.
- **Impact:** None — vitest correctly fails with `Cannot find module` for the dynamic import path.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. Test harness files only — no runtime code.

## Known Stubs

All four stubs are intentionally stub-only at this stage. They are designed to be RED; their purpose is to be driven GREEN by downstream plans:

| Stub | File | Reason |
|------|------|--------|
| Missing `src/scripts/hash-password` | `tests/hash-password.test.ts:9` | Plan 02 creates this module |
| Missing `docker-compose.yml` | `tests/compose-lint.sh:12` | Plan 05 creates this file |
| Missing `scripts/entrypoint-app.sh` | `tests/entrypoint.test.sh:14` | Plan 04 creates this file |
| Missing `docker-compose.yml` | `scripts/stack-smoke.sh:11` | Plan 05 creates this file |

These stubs are intentional per plan design — they are NOT defects.

## Commits

| Task | Commit | Message |
|------|--------|---------|
| Task 1 | 969a997 | test(999.2-01): add vitest config + hash-password RED stub |
| Task 2 | 4260c7c | test(999.2-01): add compose-lint.sh RED stub |
| Task 3 | b75b340 | test(999.2-01): add entrypoint.test.sh + stack-smoke.sh RED stubs |

## Self-Check: PASSED

- `tests/hash-password.test.ts` — FOUND
- `tests/compose-lint.sh` — FOUND, EXECUTABLE
- `tests/entrypoint.test.sh` — FOUND, EXECUTABLE
- `scripts/stack-smoke.sh` — FOUND, EXECUTABLE
- `vitest.config.ts` — FOUND, contains `tests/**/*.{test,spec}.ts` and `@` alias
- Commits 969a997, 4260c7c, b75b340 — all present in git log
