---
phase: 999.2
plan: "02"
subsystem: containerization-foundation
tags: [config, bcrypt, seed, scripts, dependencies]
dependency_graph:
  requires: [999.2-01]
  provides: [hash-password-helper, seed-admin-boot, config-containerization-vars]
  affects: [999.2-04-entrypoint, 999.2-06-caddy-admin]
tech_stack:
  added: []
  patterns: [zod-env-extension, bcrypt-hash-helper, idempotent-seed, dotenv-fallback]
key_files:
  created:
    - src/scripts/hash-password.ts
    - src/scripts/seed-admin-boot.ts
  modified:
    - src/lib/config.ts
    - .env.example
    - package.json
    - package-lock.json
decisions:
  - drizzle-kit and tsx promoted to dependencies for standalone runner image (RESEARCH.md Pitfall 1)
  - ADMIN_PASSWORD_HASH validated with bcrypt regex before insert — fail-loud on garbage input
  - seed-admin-boot uses return (not exit) on missing admin vars — graceful no-op
  - require.main === module gate in hash-password.ts prevents CLI auto-exec when imported by tests
metrics:
  duration: "~8min"
  completed: "2026-04-18"
  tasks: 3
  files: 6
---

# Phase 999.2 Plan 02: Foundation — Config, Scripts, Dependencies Summary

**One-liner:** Extended zod env schema with 6 containerization vars, shipped bcrypt hash helper CLI+library, created non-interactive idempotent admin seed script, promoted drizzle-kit+tsx to dependencies.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create hash-password.ts (turns test GREEN) | 3005f56 | src/scripts/hash-password.ts, package.json, package-lock.json |
| 2 | Extend config.ts + .env.example | efdf6c5 | src/lib/config.ts, .env.example |
| 3 | Create seed-admin-boot.ts | 9b27e29 | src/scripts/seed-admin-boot.ts |

## New Env Vars in config.ts

| Var | Type | Default | Purpose |
|-----|------|---------|---------|
| POSTGRES_PASSWORD | `z.string().min(8).optional()` | — | Compose postgres service password |
| ADMIN_USERNAME | `z.string().min(1).optional()` | — | First-boot admin seed |
| ADMIN_PASSWORD_HASH | `z.string().regex(/^\$2[aby]\$\d{2}\$/).optional()` | — | Pre-hashed bcrypt password for seed |
| CADDY_ADMIN_URL | `z.string().url()` | `http://caddy:2019` | Caddy Admin API endpoint |
| CADDY_INTERNAL_PORT | `z.coerce.number().int()` | `8080` | Caddy listen port (host nginx proxies here) |
| DOCKER_GID | `z.coerce.number().int().optional()` | — | Docker socket GID (runtime-detected if absent) |

## Verification Results

- `npx vitest run tests/hash-password.test.ts` → PASSED (Plan 01 RED test is now GREEN)
- `npx tsc --noEmit` → 0 errors
- `npm run hash-password -- testpw` → prints `$2b$12$...` hash
- `jq '.dependencies["drizzle-kit"]' package.json` → `"^0.31.10"` (in dependencies)
- `jq '.devDependencies["drizzle-kit"]' package.json` → `null` (removed from devDependencies)
- `jq '.dependencies["tsx"]' package.json` → `"^4.21.0"` (in dependencies)

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Follow-up Note (out of scope for this plan)

`dotenv` is still in `devDependencies` (`^17.4.1`). `seed-admin-boot.ts` imports dotenv as a runtime fallback for local execution. In the Docker container, dotenv is a no-op (env vars injected by compose), but if the standalone image doesn't include devDependencies, this could fail locally. Plan 03 (Dockerfile) or Plan 04 (entrypoint) should verify dotenv is available at runtime. This is NOT auto-fixed here per plan scope constraints.

## Threat Surface Scan

No new network endpoints, auth paths, or file access patterns introduced. `ADMIN_PASSWORD_HASH` is a bcrypt hash (not plaintext) — never logged. `.env.example` uses obvious placeholder `change-me-in-env-local`. Threat model mitigations T-999.2-03, T-999.2-04, T-999.2-06 all implemented as specified.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| src/scripts/hash-password.ts exists | FOUND |
| src/scripts/seed-admin-boot.ts exists | FOUND |
| src/lib/config.ts exists | FOUND |
| .env.example exists | FOUND |
| commit 3005f56 exists | FOUND |
| commit efdf6c5 exists | FOUND |
| commit 9b27e29 exists | FOUND |
| vitest run tests/hash-password.test.ts | 1 passed |
