---
phase: 999.2
plan: 10
type: execute
wave: 7
depends_on: [999.2-01, 999.2-03, 999.2-04, 999.2-05, 999.2-06, 999.2-07, 999.2-08, 999.2-09]
files_modified:
  - .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "`tests/compose-lint.sh` exits 0 (both base + merged compose configs validate)"
    - "`tests/entrypoint.test.sh` exits 0 (idempotent seed + drizzle-kit push works against a throwaway postgres)"
    - "`scripts/stack-smoke.sh` exits 0 (all four services come up healthy and respond to their probes)"
    - "`npx vitest run` passes the full suite including caddy-admin + compose-generator regression tests"
    - "`npx tsc --noEmit` exits 0 across the whole repo"
    - "After a successful smoke, running it a SECOND time still passes (migration + seed idempotency proven end-to-end)"
    - "`999.2-VALIDATION.md` per-task map is populated with the actual test commands each task from Plans 01-09 satisfies"
  artifacts:
    - path: "999.2-VALIDATION.md"
      provides: "Updated per-task verification map with actual test commands filled in"
  key_links:
    - from: "docker compose up --wait"
      to: "all four service healthchecks"
      via: "stack-smoke.sh"
      pattern: "up -d --build --wait"
---

<objective>
End-to-end verification gate for Phase 999.2. Runs all three shell smokes (compose-lint, entrypoint, stack-smoke) plus the full vitest suite, then updates `999.2-VALIDATION.md`'s per-task map and flips `nyquist_compliant: true`. The [BLOCKING] schema push requirement is validated here: the stack-smoke's `docker compose up -d --build --wait` run proves migrations executed successfully via the entrypoint.

**This plan is `autonomous: false`** because it requires a working Docker daemon to execute `docker compose up`. Executor environments (cloud sandboxes) may not have Docker. If the executor cannot run the smoke commands, the plan is marked as `autonomous: false` with explicit manual instructions: the user runs `scripts/stack-smoke.sh` locally on a machine with Docker, pastes the result into the SUMMARY, and signs off.

Cross-reference (informational): gate for the whole phase. No individual requirement tied to it; it's the integration signal.

Purpose: Close the phase with a single green-button verification that exercises every earlier plan's output together.
Output: One documentation update (`999.2-VALIDATION.md` populated + `nyquist_compliant: true`) plus a SUMMARY file recording the smoke result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-devdock-self-containerization/999.2-CONTEXT.md
@.planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md
@tests/compose-lint.sh
@tests/entrypoint.test.sh
@scripts/stack-smoke.sh
@docker-compose.yml
@scripts/entrypoint-app.sh

<interfaces>
<!-- Plan 01 → stubs created (RED) -->
<!-- Plan 02 → hash-password.test.ts GREEN -->
<!-- Plan 03 → Dockerfile + Caddyfile -->
<!-- Plan 04 → entrypoint scripts (drives entrypoint.test.sh potential GREEN) -->
<!-- Plan 05 → compose files (drives compose-lint.sh GREEN) -->
<!-- Plan 06 → caddy-admin tests GREEN -->
<!-- Plan 07 → compose-generator regression test GREEN -->
<!-- Plan 08 → lifecycle wiring (typecheck only — exercised E2E in stack-smoke via env create) -->
<!-- Plan 09 → deploy artifact edits + HUMAN-UAT (not executed by this plan; runbook for user) -->

<!-- 999.2-VALIDATION.md has a draft per-task map at line ~39 awaiting population -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run full automated suite (manual gate if Docker unavailable in sandbox)</name>
  <files>(no file writes — this is a verification gate)</files>
  <read_first>
    - tests/compose-lint.sh
    - tests/entrypoint.test.sh
    - scripts/stack-smoke.sh
    - .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md
  </read_first>
  <action>
    This is a manual verification checkpoint. The executor (if Docker-enabled) runs the 5-step command sequence in <how-to-verify> and reports results. If the executor sandbox lacks Docker, the user runs the commands manually on a Docker-enabled host and signs off via <resume-signal>.

    The acceptance is: all 5 command groups exit 0 (static checks + compose-lint + entrypoint.test + stack-smoke × 2 runs for idempotency).
  </action>
  <what-built>
    All artifacts from Plans 01-09 are in place:
    - Wave 0 stubs driven to GREEN by later waves
    - Container images buildable (Dockerfile + entrypoints)
    - Full compose stack (4 services + 2 networks + 3 volumes)
    - Caddy Admin API client wired into environment lifecycle
    - Deploy artifacts updated with Caddy upstream + deprecation headers
    - HUMAN-UAT.md runbook ready for VPS cutover
  </what-built>
  <how-to-verify>
    **Run these commands in order on a machine with Docker available (local dev box or CI runner):**

    ```bash
    # Step A — repository is at the current HEAD of phase 999.2; ensure deps installed:
    npm ci

    # Step B — static checks first (fast):
    npx tsc --noEmit                       # must exit 0
    npm run lint                           # must exit 0
    npx vitest run                         # must exit 0 — runs caddy-admin + compose-generator + hash-password tests
    # Expected: at least 12+ tests pass (hash-password + caddy-admin ~9 + compose-generator existing + new devdock-proxy case)

    # Step C — compose config validates:
    bash tests/compose-lint.sh             # must exit 0

    # Step D — entrypoint + stack smoke (requires Docker):
    bash tests/entrypoint.test.sh          # must exit 0 — proves idempotent seed + drizzle push against throwaway postgres

    # Ensure .env.local has at minimum: POSTGRES_PASSWORD, AUTH_SECRET, ADMIN_USERNAME, ADMIN_PASSWORD_HASH
    # (use .env.example as the template; generate hash via `npm run hash-password -- <pw>`).

    bash scripts/stack-smoke.sh            # must exit 0 — full `docker compose up -d --build --wait` + all 4 probes

    # Step E — idempotency re-run:
    bash scripts/stack-smoke.sh            # second run must also exit 0 (migration + seed idempotent)
    ```

    **If Docker is unavailable in the executor sandbox:**
    - Steps A-C (static + compose-lint) can still run autonomously
    - Steps D-E (entrypoint.test + stack-smoke) MUST be run manually by the user on a Docker-enabled host
    - Paste the output of Step D and Step E into the SUMMARY for this plan — the output proves the gate passed

    **Expected passing output (abbreviated):**
    ```
    [2026-04-18T...] entrypoint.test: PASS
    [2026-04-18T...] stack-smoke: PASS
    [2026-04-18T...] stack-smoke: PASS    ← second run also PASS
    ```
  </how-to-verify>
  <resume-signal>
    Type "approved — all 5 steps green" OR describe the first failing step with its output (command, exit code, last 40 lines of stderr/stdout).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Populate 999.2-VALIDATION.md per-task map + mark nyquist_compliant</name>
  <files>.planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md</files>
  <read_first>
    - .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md (current draft — especially the "Per-Task Verification Map" placeholder row and the "Validation Sign-Off" checkboxes)
    - All 999.2-NN-PLAN.md files in this phase directory (source of task IDs + automated commands)
  </read_first>
  <action>
    Open `.planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md` and make three edits:

    **Edit 1 — frontmatter.** Change these fields at the top:
    - `status: draft` → `status: approved`
    - `nyquist_compliant: false` → `nyquist_compliant: true`
    - `wave_0_complete: false` → `wave_0_complete: true`

    **Edit 2 — Per-Task Verification Map.** Replace the single placeholder row with the full map. Enumerate every task from Plans 01-09 by scanning each PLAN.md file's `<task>` blocks. For each task, record: Plan NN, Task number, Wave, the most-specific `<automated>` command, and the file(s) the task produces.

    Use this markdown table structure (replace the current row between `|---...` and the blank-line that follows):

    ```
    | Task ID | Plan | Wave | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 01-T1 | 01 | 1 | T-999.2-03 | vitest stubs isolate tests from runtime | RED stub | `npx vitest run tests/hash-password.test.ts` | Yes | ✅ green (after Plan 02) |
    | 01-T2 | 01 | 1 | — | — | RED stub | `bash tests/compose-lint.sh` | Yes | ✅ green (after Plan 05) |
    | 01-T3 | 01 | 1 | — | — | RED stub | `bash tests/entrypoint.test.sh` + `bash scripts/stack-smoke.sh` | Yes | ✅ green (after Plan 05) |
    | 02-T1 | 02 | 1 | — | hashPassword + CLI guards | unit | `npx vitest run tests/hash-password.test.ts` | Yes | ✅ green |
    | 02-T2 | 02 | 1 | T-999.2-03, T-999.2-04 | zod min-length on POSTGRES_PASSWORD; bcrypt shape on ADMIN_PASSWORD_HASH | static | `npx tsc --noEmit` + grep spot-checks | Yes | ✅ green |
    | 02-T3 | 02 | 1 | T-999.2-03, T-999.2-06 | idempotent seed; hash-shape validation; no plaintext logging | unit-ish | `npx tsc --noEmit` (E2E via entrypoint.test.sh) | Yes | ✅ green |
    | 03-T1 | 03 | 2 | T-999.2-01, T-999.2-03, T-999.2-05, T-999.2-06 | node:22-slim base; no Alpine; gosu + postgresql-client present; drizzle-kit explicitly copied | static | grep + `docker build` (Plan 10 stack-smoke) | Yes | ✅ green |
    | 03-T2 | 03 | 2 | T-999.2-02 | admin :2019 wildcard bind within compose-internal only; auto_https off | static | grep (E2E via stack-smoke) | Yes | ✅ green |
    | 04-T1 | 04 | 2 | T-999.2-03, T-999.2-05, T-999.2-06 | postgres-wait + runtime GID fixup + idempotent seed + `exec gosu`; no --force on drizzle-kit push | unit + E2E | `bash tests/entrypoint.test.sh` | Yes | ✅ green |
    | 04-T2 | 04 | 2 | T-999.2-05 | GID fixup-only, no DB dep for terminal | static | `bash -n scripts/entrypoint-terminal.sh` | Yes | ✅ green |
    | 05-T1 | 05 | 3 | T-999.2-01, T-999.2-02, T-999.2-04 | socket scoping enforced by structure; no 2019:2019; postgres has no ports | static | `bash tests/compose-lint.sh` | Yes | ✅ green |
    | 05-T2 | 05 | 3 | — | dev override reuses deps stage; HMR bind-mounts | static | `bash tests/compose-lint.sh` (merged) | Yes | ✅ green |
    | 06-T1 | 06 | 4 | T-999.2-02 | allow-list regex validation on all inputs | unit RED | `npx vitest run src/lib/docker/__tests__/caddy-admin.test.ts` | Yes | ✅ green (after 06-T2) |
    | 06-T2 | 06 | 4 | T-999.2-02 | DELETE-then-POST upsert; 404-is-success delete; dynamic serverKey cache | unit | `npx vitest run src/lib/docker/__tests__/caddy-admin.test.ts` | Yes | ✅ green |
    | 07-T1 | 07 | 4 | T-999.2-01 | devdock-proxy external; dev attaches without expanding socket scope | static | `grep devdock-proxy docker/templates/base-compose.yml` | Yes | ✅ green |
    | 07-T2 | 07 | 4 | — | regression test locks the contract | unit | `npx vitest run src/lib/docker/__tests__/compose-generator.test.ts` | Yes | ✅ green |
    | 07-T3 | 07 | 4 | — | doc-only JSDoc note | static | `grep "Phase 999.2 (D-10)" src/lib/docker/compose-generator.ts` | Yes | ✅ green |
    | 08-T1 | 08 | 5 | T-999.2-02 | helpers double-validate via caddy-admin regexes | static | `npx tsc --noEmit` | Yes | ✅ green |
    | 08-T2 | 08 | 5 | T-999.2-02 | deregister idempotent | static | `npx tsc --noEmit` | Yes | ✅ green |
    | 08-T3 | 08 | 5 | — | annotation-only | static | grep | Yes | ✅ green |
    | 09-T1 | 09 | 5 | — | nginx upstream updated | static | grep `127.0.0.1:8080` in deploy/nginx/*.conf | Yes | ✅ green |
    | 09-T2 | 09 | 5 | — | systemd deprecation header | static | `head -5 deploy/systemd/devdock.service \| grep DEPRECATED` | Yes | ✅ green |
    | 09-T3 | 09 | 5 | T-999.2-03, T-999.2-07 | runbook prescribes chmod 0600 + pre-created host dirs | manual | user reads + runs HUMAN-UAT.md | Yes | 📄 doc-only |
    | 10-T1 | 10 | 7 | all | full smoke proves migration + seed + 4-service health | E2E | `bash scripts/stack-smoke.sh` (2×) | — | ✅ green (from Task 1) |
    ```

    **Edit 3 — Validation Sign-Off.** Change the checkbox lines at the bottom:
    - `[ ]` → `[x]` for all 6 sign-off checkboxes
    - `**Approval:** pending` → `**Approval:** signed off 2026-04-18 via Plan 10 smoke gate`
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md &amp;&amp; grep -q "wave_0_complete: true" .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md &amp;&amp; grep -q "signed off" .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md &amp;&amp; grep -c "10-T1\|09-T3\|08-T1" .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md | tr -d ' ' | grep -q "^3$"</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter: `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`
    - Per-Task Verification Map contains rows for at least: 01-T1..T3, 02-T1..T3, 03-T1..T2, 04-T1..T2, 05-T1..T2, 06-T1..T2, 07-T1..T3, 08-T1..T3, 09-T1..T3, 10-T1 (total ~22 rows)
    - Each row references a specific automated command (no "TBD" values remain)
    - All six checkboxes in "Validation Sign-Off" are ticked `[x]`
    - Sign-off line contains `signed off 2026-04-18`
  </acceptance_criteria>
  <done>
    VALIDATION.md is the phase's verification contract — now populated with ground truth and marked compliant. Future audit can trace any phase behavior back to the test that proved it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan is a verification gate.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.2-01 | Elevation of Privilege | stack-smoke validation | mitigate | `scripts/stack-smoke.sh` brings the stack up via `docker compose up`, exercising the compose file's socket-mount scoping (socket on app+terminal only, NOT on caddy/postgres). If any misconfiguration slipped through, the smoke would catch it. |
| T-999.2-02 | Tampering, DoS | stack-smoke Caddy probes | mitigate | Smoke probes `http://caddy:2019/config/` from INSIDE the app container (confirms admin reachable internally). Does NOT probe `curl http://localhost:2019` from host — if that succeeded, port 2019 leaked; the smoke would exit 0 regardless, so the acceptance criteria grep-check in Plan 05 is the actual gate. |
| T-999.2-06 | Tampering | stack-smoke second-run | mitigate | Running `stack-smoke.sh` twice exercises idempotent migration + seed — if drizzle-kit push were destructive or the seed inserted duplicates, the second run would fail. Defence-in-depth for T-999.2-06 (already mitigated by entrypoint design in Plan 04). |
</threat_model>

<verification>
1. Task 1's checkpoint is signed off with either "approved — all 5 steps green" or a detailed failure pointing at a specific plan
2. `grep nyquist_compliant .planning/phases/999.2-devdock-self-containerization/999.2-VALIDATION.md` → `true`
3. Every task ID from Plans 01-09 appears at least once in the Per-Task Map
</verification>

<success_criteria>
- All three shell smokes exit 0 (compose-lint, entrypoint.test, stack-smoke × 2 runs for idempotency)
- `npx vitest run` passes the full suite
- `npx tsc --noEmit` passes
- 999.2-VALIDATION.md populated and marked `nyquist_compliant: true`
- SUMMARY for this plan records the smoke output verbatim (paste-and-sign pattern for Task 1)
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-devdock-self-containerization/999.2-10-SUMMARY.md` containing:
- The literal output of `bash scripts/stack-smoke.sh` (both runs — the idempotency proof)
- The literal output of `bash tests/entrypoint.test.sh`
- The output of `npx vitest run` (reporter summary is sufficient — no full stack traces needed for passing tests)
- Confirmation that `999.2-VALIDATION.md` is marked compliant
- Any smoke-detected issues + their resolution (expected: none; if any, queue a gap-closure plan)
</output>
