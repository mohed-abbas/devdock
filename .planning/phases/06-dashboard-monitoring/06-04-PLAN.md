---
phase: 06-dashboard-monitoring
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "Database schema is pushed and previewPort column exists in the live database"
    - "Next.js application builds without errors"
    - "Test suite passes (minus pre-existing compose-generator failure)"
    - "Dashboard renders with two sections visible"
    - "Environment cards show Logs and Preview buttons when applicable"
    - "Create dialog accepts preview port"
  artifacts: []
  key_links: []
---

<objective>
Integration gate: push database schema, verify build, run test suite, and human-verify the complete dashboard experience.

Purpose: Ensure all Phase 6 changes integrate correctly. The schema push is BLOCKING — types come from config, not the live database, so build/type checks pass without it, creating a false-positive verification state.
Output: Schema pushed, build verified, tests green, human confirmation of dashboard UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-monitoring/06-CONTEXT.md
@.planning/phases/06-dashboard-monitoring/06-01-SUMMARY.md
@.planning/phases/06-dashboard-monitoring/06-02-SUMMARY.md
@.planning/phases/06-dashboard-monitoring/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Schema push, build verification, and test suite</name>
  <files></files>
  <read_first>
    src/lib/db/schema.ts,
    drizzle.config.ts,
    vitest.config.ts,
    package.json
  </read_first>
  <action>
**1. Push database schema (BLOCKING — must complete before verification):**

Run the Drizzle schema push command to sync the live database with the schema.ts definition. This adds the `preview_port` nullable integer column to the `environments` table.

```bash
npx drizzle-kit push
```

If this command prompts interactively (asking to confirm destructive changes), the column addition is non-destructive (nullable column, no data migration), so it should proceed without prompts. If it does prompt, answer affirmatively.

Verify the column was added:
```bash
# Connect to the devdock database and check the environments table
psql "$DATABASE_URL" -c "SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'environments' AND column_name = 'preview_port';"
```

Expected output: `preview_port | integer | YES`

**2. Verify Next.js production build:**

```bash
cd /home/mohed_abbas/murx/perso/claudecodevps && npx next build
```

This must complete without errors. Warnings are acceptable (e.g., dynamic routes, server actions).

**3. Run full test suite:**

```bash
cd /home/mohed_abbas/murx/perso/claudecodevps && npx vitest run --reporter=verbose
```

All tests must pass except the pre-existing `compose-generator.test.ts` failure (1 test). If any NEW test failures appear, investigate and fix before proceeding.

**4. TypeScript check:**

```bash
cd /home/mohed_abbas/murx/perso/claudecodevps && npx tsc --noEmit
```

Must exit 0 with no errors.
  </action>
  <verify>
    <automated>cd /home/mohed_abbas/murx/perso/claudecodevps && npx tsc --noEmit && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` completes successfully
    - psql query confirms `preview_port` column exists with type `integer` and `is_nullable = YES`
    - `npx next build` exits 0
    - `npx vitest run` shows all tests passing (except pre-existing compose-generator failure)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Schema pushed to live database with previewPort column, Next.js builds cleanly, test suite green, TypeScript compiles without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify dashboard UI</name>
  <files></files>
  <action>
Present the complete Phase 6 dashboard to the user for visual and functional verification. The following features were built across Plans 01-03:

1. Dashboard page restructured into "Dev Environments" + "Production Apps" sections
2. Environment cards with new Logs button (visible when running) and Preview button (visible when running + preview port set)
3. Create Environment dialog with optional Preview Port field
4. Logs page at /dashboard/env/[id]/logs with full-screen log viewer, auto-scroll toggle, clear button
5. Production app cards with status, uptime, ports, and blue "Production" badge (read-only)
6. Preview proxy that forwards /api/environments/[id]/preview/* to container internal IP
  </action>
  <verify>
    Human verification steps:

    Start the dev server if not running: `npm run dev`

    1. **Dashboard layout**: Navigate to https://devdock.yourdomain.com/dashboard (or localhost:3000/dashboard).
       - Verify "Dev Environments" heading (not "Environments")
       - Verify "Production Apps" section appears below with separator (if PRODUCTION_APPS_DIR is configured)
       - If no production apps: section should be completely absent

    2. **Create Environment with Preview Port**:
       - Click "New Environment"
       - Verify "Preview Port" field exists below the branch selector
       - Verify placeholder shows "3000"
       - Verify helper text: "Port your app listens on inside the container (optional)"
       - Create an environment with preview port 3000

    3. **Environment Card Buttons** (with a running environment):
       - Verify Logs button (ScrollText icon) appears when running
       - Verify Preview button (ExternalLink icon) appears when running AND preview port was set
       - Verify button order left-to-right: Preview, Logs, Terminal, Start/Stop, Delete
       - Verify Preview button opens in new tab

    4. **Logs Page**:
       - Click the Logs button on a running environment
       - Verify full-screen layout with header bar (back button, env name, status badge)
       - Verify auto-scroll toggle button (ArrowDownToLine icon) in header
       - Verify clear button (Trash2 icon) in header
       - Verify log lines appear if container is producing output
       - Verify "Waiting for log output..." shows when no logs yet

    5. **Production Apps** (if configured):
       - Verify production cards show app name, status badge, uptime, exposed ports
       - Verify blue "Production" badge on each card
       - Verify NO action buttons on production cards (read-only)

    Resume signal: Type "approved" or describe any issues found
  </verify>
  <done>
    User has visually confirmed all Phase 6 dashboard features work correctly: two-section layout, enhanced environment cards, preview port creation, logs page, production app monitoring.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Integration verification — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Information Disclosure | Schema push | accept | Schema push only adds nullable column; no data exposure risk |
</threat_model>

<verification>
- Schema push confirmed via psql
- `npx next build` exits 0
- `npx vitest run` green (minus known failure)
- `npx tsc --noEmit` exits 0
- Human confirms dashboard UI matches expected behavior
</verification>

<success_criteria>
- Database has preview_port column
- Application builds and deploys cleanly
- All Phase 6 features visible and functional in the browser
- Human approves the dashboard experience
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-monitoring/06-04-SUMMARY.md`
</output>
